#include <iostream>
using namespace std;
int main()
{
    int a, b, c;
    cout << "enter the marks:";
    cin >> a >> b >> c;
    int avg;
    avg = (a + b + c) / 3;
    cout << "average of three number:" << avg;
    return 0;
}
